# For local dev (points to local server)
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# For production (points to your deployed socket server on Railway/Render)
# NEXT_PUBLIC_SOCKET_URL=https://your-server.railway.app
